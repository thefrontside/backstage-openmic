---
layout: base
---

<main id="main" class="main inner box-flex pt-1000 pb-300" tabindex="-1">
  <header class="hero page-header laptop:box-flex">
    <div class="laptop:width-half">
      <h1 class="hero--title">
        {{ title }}
      </h1>
      <p class="hero--description">{{ meta_description }}</p>

      <span class="gradient-line mt-1000 mb-800"></span>
      {# <svg class="scrolling-text--item" version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
                <text class="stroke-text" x="50%" y="50%">
                    {{ eventDateUS  }} @ {{ eventTimeEST }}
                </text>
            </svg> #}

    </div>
    <div class="hero-illustration laptop:width-half">
      {# <img src="/images/hero-header@1.5x.png" alt="" /> #}
    </div>
  </header>
</main>

<div class="main inner box-flex pt-400 pb-900">
  <h3 class="mb-400">Talk recordings
    <img src="/images/decor-eye.png" alt="" class="inline-img-decor"/></h3>
  <ol class="talks-list">
    {# <li class="talks-item laptop:box-flex">
            <div class="talk-video">
                <iframe src="https://player.vimeo.com/video/536715621?badge=0&amp;autopause=0&amp;player_id=0&amp;app_id=58479"
                    frameborder="0"
                    allow="autoplay; fullscreen; picture-in-picture"
                    allowfullscreen
                    style="position:absolute;top:0;left:0;width:100%;height:100%;"
                    title="Roadie&amp;#039;s Multi-Tenant Backstage Architecture">
                </iframe>
            </div>
            <script src="https://player.vimeo.com/api/player.js"></script>
        </li> #}
    {% for talk in talks %}
      <li class="talks-item laptop:box-flex">
        <div class="talk-box laptop:box-flex align-center">
          <div class="speaker-avatar speaker-avatar__talk">
            <img src="{{talk.data.speaker.image}}" alt="{{ talk.data.speaker.name }} portrait" class="speaker-avatar--speaker-pic"/>
            {%if talk.data.speaker.companyLogo %}
              <img src="{{talk.data.speaker.companyLogo}}" alt="{{ talk.data.speaker.company }} logo" class="speaker-avatar--company-icon"/>
            {% endif %}
          </div>
          <div class="talk-box--text">
            <h3 class="mb-300">
              {{talk.data.title}}
            </h3>
            <strong>
              {{ talk.data.speaker.name }},
              {{ talk.data.speaker.title }}
              {{"of" if talk.data.speaker.isLibrary else "at" }}
              <a href="{{ talk.data.speaker.companyLink }}" target="_blank" rel="noopener">{{ talk.data.speaker.company }}</a>
            </strong>
            <p class="mt-600 mb-500">
              {{talk.data.description}}
            </p>
            {% if talk.data.vimeoLink %}
              <a href="{{ talk.url }}" class="button button__secondary" title="Watch {{ talk.data.speaker.name }}'s Talk">
                Watch Talk
              </a>
            {% else %}
              <a class="button button__coming-soon" disabled title="Watch {{ talk.data.speaker.name }}'s Talk">
                Recording coming soon
              </a>
            {% endif %}
            {# <a href="{{ talk.url }}" class="button button__secondary" title="Watch {{ talk.data.speaker.name }}'s Talk">
                        Watch Talk
                    </a> #}
            {# {% if talk.data.learnMore %}
                        <p class="text-400">
                            <a href="{{ alk.data.learnMore }}" target="_blank" rel="noopener nofollow">
                                Learn more on Github
                            </a>
                        </p>
                    {% endif %} #}
          </div>
        </div>
      </li>
    {% endfor %}
  </ol>
</div>
